import React, { Component } from 'react';
import { connect } from 'react-redux';
import actions from '../../state/actions';
import {
    FormGroup,
    NumericInput,
    Button,
    Dialog,
    Classes,
    Tooltip,
    AnchorButton,
    Intent,
    Icon,
} from '@blueprintjs/core';

class ModelClassInputs extends Component {
    render() {
        return null;
    }
}


const mapDispatchToProps = {

};

const mapStateToProps = (state) => ({

});



export default connect(mapStateToProps, mapDispatchToProps)(ModelClassInputs);
