from distutils.core import setup

setup(
  name = 'modelflow',
  packages = ['modelflow'],
  version = '0.1',
  description = 'Modelflow',
  url = 'https://github.com/ModelFlow/modelflow',
  keywords = ['modelflow'],
  classifiers = []
)